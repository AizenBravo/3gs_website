import React, { ReactNode } from 'react'

const SimpleCard = ({title, content}: {
    title: string,
    content: string | ReactNode
    borderRadius?: number
    hslBackground?: number
    opacity: number
}) => {
    const cardStyle = '';

  return (
    <div >SimpleCard</div>
  )
}

export default SimpleCard